---

name: Run Solutions
on:
  push:


jobs:
  run:
    name: Run solutions
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: 3.8
      - run: make all
